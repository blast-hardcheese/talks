:power

import replesent._
val pres = REPLesent(intp=intp, slideCounter=true, slideTotal=true)
import pres._

def revert: Unit = intp.interpret("""
  import Presentation._
  import uk.gov.hmrc.emailaddress.EmailAddress
""")

revert

def buildLift(i: Int): Unit = {
  val s = genLift(i)
  println(s)
  intp.interpret(s)
}

def end: Unit = {
  pres.last
  pres.previous
  pres.previous
}

f
